name: Test Workflow
on:
  workflow_dispatch:

jobs:
  annotations:
    runs-on:
      group: test-runner-group 
      labels: self-hosted
    steps:
      - name: Echoing only
        run: |
          echo "Hello World!"